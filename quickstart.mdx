---
title: "Quickstart"
description: "Get your API key and make your first request in under 5 minutes."
---

## Popular quickstarts

<Tabs>
  <Tab title="Wallet Data">
    <CardGroup cols={2}>
      <Card title="Get a wallet portfolio" icon="chart-pie" href="/api-reference/wallets/get-wallets-portfolio">
        Total value, position breakdown, and daily changes for any address.
      </Card>
      <Card title="List fungible positions" icon="coins" href="/api-reference/wallets/list-wallet-positions">
        Every token a wallet holds with quantities and USD values.
      </Card>
      <Card title="View DeFi positions" icon="layer-group" href="/api-reference/wallets/list-wallet-positions">
        Lending, staking, and liquidity positions across protocols.
      </Card>
      <Card title="NFT positions" icon="image" href="/api-reference/wallets/list-wallet-nft-positions">
        NFT holdings with metadata, floor prices, and collection info.
      </Card>
    </CardGroup>
  </Tab>
  <Tab title="Transactions">
    <CardGroup cols={2}>
      <Card title="Get parsed transactions" icon="clock-rotate-left" href="/api-reference/wallets/list-wallet-transactions">
        Human-readable transaction history with filters by type, chain, and date.
      </Card>
      <Card title="Subscribe to webhooks" icon="bell" href="/api-reference/tx-subscriptions/create-subscription-wallet-transactions">
        Instant notifications when wallet transactions occur — no polling.
      </Card>
    </CardGroup>
  </Tab>
  <Tab title="Market Data">
    <CardGroup cols={2}>
      <Card title="Search fungible tokens" icon="magnifying-glass" href="/api-reference/fungibles/list-fungibles">
        Look up any token by name, get prices, charts, and market data.
      </Card>
      <Card title="Token charts" icon="chart-line" href="/api-reference/fungibles/get-fungible-chart">
        Historical price and market cap charts for any token.
      </Card>
    </CardGroup>
  </Tab>
  <Tab title="Trading">
    <CardGroup cols={2}>
      <Card title="Get swap quotes" icon="arrow-right-arrow-left" href="/api-reference/swap/swap-fungibles">
        Fetch the best swap and bridge offers across chains.
      </Card>
      <Card title="Check gas prices" icon="gas-pump" href="/api-reference/gas-prices/list-gas-prices">
        Current gas prices across all supported chains.
      </Card>
    </CardGroup>
  </Tab>
</Tabs>

---

## Make your first request

<Steps>
  <Step title="Get your API key">
    Sign up at [developers.zerion.io](https://developers.zerion.io) and create an API key from the dashboard. It's free to start.
  </Step>
  <Step title="Try it from the docs">
    The fastest way to test the API is right here in the docs. Open any [API reference](/api-reference/wallets/get-wallets-portfolio) page, enter your API key in the **Username** field, leave **Password** empty, and hit **Send**.
  </Step>
  <Step title="Make a request from your code">
    The API uses HTTP Basic Auth — your API key is the username, password is empty. Let's fetch a wallet portfolio:

    <CodeGroup>

    ```bash cURL
    curl -u "YOUR_API_KEY:" \
      "https://api.zerion.io/v1/wallets/0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045/portfolio"
    ```

    ```javascript JavaScript
    const apiKey = "YOUR_API_KEY";
    const address = "0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045";

    const response = await fetch(
      `https://api.zerion.io/v1/wallets/${address}/portfolio`,
      { headers: { Authorization: `Basic ${btoa(`${apiKey}:`)}` } }
    );

    const { data } = await response.json();
    console.log(`Total positions: ${data.attributes.total.positions}`);
    ```

    ```python Python
    import requests

    api_key = "YOUR_API_KEY"
    address = "0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045"

    response = requests.get(
        f"https://api.zerion.io/v1/wallets/{address}/portfolio",
        auth=(api_key, "")
    )

    data = response.json()["data"]
    print(f"Total positions: {data['attributes']['total']['positions']}")
    ```

    </CodeGroup>
  </Step>
  <Step title="Understand the response">
    The API returns data in [JSON:API](https://jsonapi.org/) format. Every response has a `data` object with `type`, `id`, and `attributes`:

    ```json
    {
      "data": {
        "type": "portfolios",
        "id": "0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045",
        "attributes": {
          "positions_distribution_by_type": {
            "wallet": 0.85,
            "deposited": 0.10,
            "staked": 0.04,
            "locked": 0.01
          },
          "total": { "positions": 142 },
          "changes": {
            "absolute_1d": 1250.50,
            "percent_1d": 0.02
          }
        }
      }
    }
    ```

    List endpoints return a `data` array and support [pagination](/pagination-and-filtering).
  </Step>
</Steps>

## Next steps

<CardGroup cols={2}>
  <Card title="Authentication" icon="lock" href="/authentication">
    API key encoding and security best practices.
  </Card>
  <Card title="Pagination & Filtering" icon="filter" href="/pagination-and-filtering">
    Cursor pagination, filters, sorting, and currency options.
  </Card>
  <Card title="Recipes" icon="book" href="/recipes">
    Step-by-step guides for common use cases.
  </Card>
  <Card title="Supported Blockchains" icon="link" href="/supported-blockchains">
    Full list of 15+ supported chains and feature coverage.
  </Card>
</CardGroup>
